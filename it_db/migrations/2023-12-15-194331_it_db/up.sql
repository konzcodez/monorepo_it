CREATE TABLE computers (
  id serial PRIMARY KEY,
  ip VARCHAR,
  name VARCHAR,
  os VARCHAR,
  snum VARCHAR,
  notes VARCHAR,
  model VARCHAR,
  manufacturer VARCHAR,
  cpu VARCHAR,
  memory VARCHAR,
  storage VARCHAR,
  install_date VARCHAR,
);
